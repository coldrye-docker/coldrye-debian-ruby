# vim: noexpandtab:ts=4:sw=4

provider = coldrye
os = debian
release = jessie
feature = ruby
feature_version = 2.1.x
tag := $(shell date +"%Y%m%d%H%M%S")
image = $(os)-$(feature)

include ../../Makefile.in

